# lombok.config
# Solo agregar las anotaciones realmente necesarias
# Permite que @Qualifier sea copiada al constructor generado por @RequiredArgsConstructor
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier